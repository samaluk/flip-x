[tools]
hk = "1.56.1"
"github:mongodb/kingfisher" = "2.0.0"

[env]
HK_MISE = "1"

[hooks]
postinstall = "hk install --mise"

[tasks.pre-commit]
description = "Run the same checks as the pre-commit hook"
run = "hk run pre-commit"

[tasks.pre-push]
description = "Run the same checks as the pre-push hook"
run = "hk run pre-push"
